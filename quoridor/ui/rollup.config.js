//import resolve from '@rollup/plugin-node-resolve';

/*export default {
  input: 'src/App.js', // Specify the entry point of your application
  output: {
    file: 'dist/bundle.js', // Specify the output file and path
    format: 'iife', // Output format suitable for use in a <script> tag
  },
  plugins: [resolve()],
};*/